## Main rules:

- Components must be reusable in different environments (telegram/extension/web)
- Components must be written in typescript
- Components must be written in react
- Components must be written in tailwindcss
