import { atom } from "jotai"

export const posSessionIdAtom = atom<string | null>(null)

export const posLinkIdAtom = atom<string | null>(null)

export const posEstablishmentIdAtom = atom<string | null>(null)

export const posCheckoutUrlAtom = atom<string | null>(null)
